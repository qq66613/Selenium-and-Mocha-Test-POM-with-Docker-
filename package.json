{
  "name": "selenium-mocha-docker",
  "version": "1.0.0",
  "description": "Automation test dengan Selenium WebDriver + Mocha + Chai + Docker (POM)",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "mocha test/*.js --timeout 30000"
  },
  "dependencies": {
    "selenium-webdriver": "^4.12.0",
    "chromedriver": "^140.0.0",
    "chai": "^5.0.0"
  },
  "devDependencies": {
    "mocha": "^10.0.0"
  }
}
